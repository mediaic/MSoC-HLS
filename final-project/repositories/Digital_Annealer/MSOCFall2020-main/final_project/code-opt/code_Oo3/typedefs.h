//===========================================================================
// typedefs.h
//===========================================================================
// @brief: This header defines the shorthand of several ap_uint data types.

#ifndef TYPEDEFS_H
#define TYPEDEFS_H

#include "ap_int.h"
#include "ap_fixed.h"


// #define DIGIT_SIZE 49

// typedef ap_uint<4> bit4;
// typedef ap_uint<6> bit6;
// typedef ap_uint<32> bit32;
// typedef ap_uint<64> bit64;
// typedef ap_uint<DIGIT_SIZE> digit;

//typedef ap_fixed<16,12> bit16;
//I want to change double to bit16, but it will cause "exp()" error.
//I change "exp()"" to "expf()", but encounter another error.

#endif
